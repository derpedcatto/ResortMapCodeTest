// MapGrid + zoom & controls wrapper inside of map box

// the full widget: the "Resort Map" title, the bordered viewport box with overflow: hidden (the white void), the TransformWrapper for pan/zoom, the MapControls (+/-/reset), and MapGrid inside all of that.
